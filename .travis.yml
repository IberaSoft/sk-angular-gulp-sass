language: node_js
node_js:
  - "4.2.4"

branches:
  only:
    - master

before_install:
  - npm install gulp -g

script:
  - npm install

cache:
  directories:
  - vendor
  - node_modules