import AppController from './app.controller';

const moduleName = 'app.controller';

angular
    .module(moduleName, [])
    .controller('AppController', AppController);

export default moduleName;